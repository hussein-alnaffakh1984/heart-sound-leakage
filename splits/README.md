# Evaluation split indices

Add the train/validation/test split index files here (e.g., CSV or .npy listing
recording IDs per split for each protocol: segment-level, record-level, cross-database/LODO).

These indices are what make the experiments exactly reproducible, since the paper's
central finding concerns how the split unit changes measured performance.
